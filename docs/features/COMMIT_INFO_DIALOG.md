# Commit Info Dialog Feature

## Overview

The commit info dialog displays build and version information including:
- Commit hash
- Commit date and time
- Commit message

## How to Access

On the main menu screen, click or tap on the version text in the bottom-left corner (e.g., "v1.0 (abc123)").

## What You'll See

A dialog titled "Build Information" will appear showing:
1. **Commit Hash:** The short git commit hash (e.g., "995bd6c")
2. **Commit Date:** The full date and time of the commit (e.g., "2026-01-14 18:32:16 +0000")
3. **Commit Message:** The commit message (e.g., "Initial plan")

## Localization

The dialog is fully localized in:
- English
- German (Deutsch)
- Spanish (Español)
- French (Français)
- Italian (Italiano)

Change the language in Settings to see the translated labels.

## Implementation Details

### BuildConfig Generation
The build system automatically generates `BuildConfig.kt` with commit information:
```kotlin
object BuildConfig {
    const val VERSION_NAME = "1.0"
    const val COMMIT_HASH = "995bd6c"
    const val COMMIT_DATE = "2026-01-14 18:32:16 +0000"
    const val COMMIT_MESSAGE = "Initial plan"
}
```

This is done via a Gradle task that runs git commands:
- `git rev-parse --short HEAD` for the hash
- `git show -s --format=%ci HEAD` for the date
- `git log -1 --pretty=%B` for the message

### UI Components
- **CommitInfoDialog.kt:** The dialog component
- **MenuScreens.kt:** Updated to make version text clickable and show the dialog

### Tests
- **CommitInfoDialogTest.kt:** Tests dialog rendering and dismissal
- **MainMenuScreenTest.kt:** Tests clicking version text and showing/dismissing the dialog

All tests pass successfully.

## Manual Verification Steps

1. Build and run the desktop application:
   ```bash
   ./gradlew :composeApp:run
   ```

2. On the main menu, look for the version text in the bottom-left corner

3. Click on the version text

4. Verify the commit info dialog appears with:
   - Title: "Build Information"
   - Three labeled fields showing commit hash, date, and message
   - A "Close" button

5. Click "Close" to dismiss the dialog

6. Repeat steps 3-5 after changing language in Settings to verify translations

## Screenshots

Screenshots can be generated by running the application and taking screenshots manually, as the automated screenshot tests encounter issues with dialog multi-root node composition in the test environment.
