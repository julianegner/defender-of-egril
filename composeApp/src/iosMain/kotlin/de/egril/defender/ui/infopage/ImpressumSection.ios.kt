package de.egril.defender.ui.infopage

import androidx.compose.runtime.Composable
import androidx.compose.ui.Modifier

/**
 * Impressum section for the info page - iOS implementation (not shown)
 */
@Composable
actual fun ImpressumSection() {
    // No impressum on iOS platform
}

/**
 * Impressum wrapper - iOS implementation (not shown)
 */
@Composable
actual fun ImpressumWrapper(rowModifier: Modifier) {
    // No impressum on iOS platform
}
