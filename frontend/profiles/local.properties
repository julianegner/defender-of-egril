# Local development profile
# Use this profile when running all services locally via Docker Compose:
#   docker compose up -d
#
# Keycloak:  http://localhost:8081  (admin / admin)
# Backend:   http://localhost:8080

# Keycloak IAM server URL
iam.base.url=http://localhost:8081

# Backend server URL
defender.backend.url=http://localhost:8080
