# Production profile (default)
# Use this profile when connecting to the production server.
# Replace the placeholder URLs below with the actual server addresses.
#
# This is the DEFAULT profile. It is used when no -Pprofile is specified:
#   ./gradlew :composeApp:run               (uses production profile)
#   ./gradlew :composeApp:run -Pprofile=local (uses local profile)
#
# You can also create additional custom profiles (e.g. profiles/staging.properties)
# by following the same format and passing the profile name to Gradle:
#   ./gradlew :composeApp:run -Pprofile=staging

# Keycloak IAM server URL
iam.base.url=https://sso.julianegner.de

# Backend server URL
defender.backend.url=https://defender-backend.egril.de

