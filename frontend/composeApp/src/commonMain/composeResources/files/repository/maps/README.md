We can compress the map images with
https://www.iloveimg.com/compress-image/compress-png
making the files smallr without losing quality. 
This is important for faster loading times and reduced memory usage, especially on mobile platforms.

Later, we should add a compression algorithm into the app itself (including the backend).
