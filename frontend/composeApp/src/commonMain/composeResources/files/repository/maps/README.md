Map images are now automatically compressed when generated by the application.
Both the frontend (desktop) and the backend use `OptimalPngEncoder` which applies
adaptive PNG row filtering (tries all 5 filter types per row) and maximum DEFLATE
compression, producing significantly smaller files without any quality loss.

Existing repository PNG files have also been optimized (128MB → 79MB, ~38% reduction).
